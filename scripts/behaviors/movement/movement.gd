extends Resource
class_name Movement


# Called once at start
# `node` is the base node representing the character
func start(node: Node) -> void:
    pass


# Called every frame
# `delta` is the elapsed time of the current frame
func update(delta: float) -> void:
    pass
